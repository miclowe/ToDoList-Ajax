<%= form_for(@todo_item) do |f| %>
  <% if @todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
      <% @todo_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %><button id="addBtn" type="submit"><span class="btnText">Add new</span></button>
  </div>

  <div class="actions">
    <%#= f.submit "Add new" %>
  </div>
  <!--
  <div class="field">
    <%#= f.label :completed %><br />
    <%#= f.check_box :completed %>
  </div>
-->
  
<% end %>